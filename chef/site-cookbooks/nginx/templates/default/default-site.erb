server {
  listen   80;
  server_name  www.simeons.net;

  access_log  <%= node[:nginx][:log_dir] %>/localhost.access.log;

  location / {
    root   /home/simeon/app;
    index  index.html index.htm;
  }
}
